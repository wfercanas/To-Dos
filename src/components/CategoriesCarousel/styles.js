import styled from 'styled-components';

const StyledCategoriesCarousel = styled.section``;

const StyledCarousel = styled.div`
  padding: 0 0 1rem;
  display: flex;
  gap: 1rem;
  overflow-x: scroll;
`;

export { StyledCategoriesCarousel, StyledCarousel };
